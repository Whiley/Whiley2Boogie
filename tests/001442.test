====
>>> main.whiley
property unique_elements<T>(T[] items, int end) -> (bool r):
    // All items upto end are unique
    return all { i in 0..end, j in (i+1)..end | items[i] != items[j] }

public export method test():
   assert unique_elements([0])
   assert unique_elements([0,1])
   assert unique_elements([0,1,2])
   assert unique_elements([1,0,2])
   assert unique_elements([0,2,1])
   assert unique_elements([2,1,0])
---
